# Environment variables example — safe to commit
# Fill these out in your local `.env` (DO NOT commit .env)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
SECRET_KEY=change-me
DEBUG=True